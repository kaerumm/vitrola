import type { Command } from '../../command'
import { CommandBuilder, type CommandDefinition } from '../../command_builder'

// export class PlayCommand implements Command {
//     buildDefinition(): CommandDefinition<any> {
//         return CommandBuilder.new('play').positional({
//             name: 'url',
//             description: 'A url that points to a music resource',
//         })
//     }
// }
